import { FC } from "react";
import CalendarComponent from "@component/main/calendar";

const Main: FC = () => {
  return (
    <div>
      <CalendarComponent />
    </div>
  );
};

export default Main;
